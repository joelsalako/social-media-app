import PageContainer from '../../components/PageContainer';
import Settings from '../../components/Settings';

export default function PreferrencesPage() {
  return (
    <PageContainer title="Preferences">
      <Settings />
    </PageContainer>
  );
}
